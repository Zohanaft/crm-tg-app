/**
 * Семантические цвета Nuxt UI (светлая / тёмная тема).
 * Позже можно менять динамически (пресеты) через тот же `ui.colors`.
 */
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      secondary: 'sky',
      neutral: 'slate',
      success: 'emerald',
      info: 'sky',
      warning: 'amber',
      error: 'rose',
    },
  },
})
